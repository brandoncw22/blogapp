import React, { useState, useEffect } from 'react';
import './styles/Home.css';

function Post(post) {
    const {id, title, content, thumbnail} = post;

    return(
        //TODO: Add post structure
    );
}

export default Post;